import React from 'react'

function ToDoList() {
  return (
    <div>
      <p>UserInfo backend Should work needs testing</p>
      <p>debate how I want checkbox to work within the table </p>
        <p>- maybe use buttons at the bottom to delete, make editable, hide? </p>

      <p>fix table sizing</p>
      <p>make things look nice</p>
      <p>see if table delete can use file delete</p>
      <p>remove signout page, change it to about us page</p>
    <p>Add in server calls for userInfo trashcan options</p>
    <p>create server calls for table edit</p>

    </div>
  )
}

export default ToDoList